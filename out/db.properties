# Database Configuration for SPARCS
# IMPORTANT: Never commit this file to version control (it's in .gitignore)

# MySQL Connection URL
# Adjust the hostname, port, and database name as needed for your environment
db.url=jdbc:mysql://localhost:3306/sparcs_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# MySQL Username
db.username=root

# MySQL Password
db.password=mabalano

